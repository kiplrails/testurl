<%= form_for(@testurl) do |f| %>
  <% if @testurl.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@testurl.errors.count, "error") %> prohibited this testurl from being saved:</h2>

      <ul>
      <% @testurl.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_field :url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
